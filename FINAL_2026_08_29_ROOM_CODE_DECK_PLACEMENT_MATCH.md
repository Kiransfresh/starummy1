# Matching Railway package

The backend keeps strict 4-digit numeric room-code normalization/validation and the same server-authoritative toss/deal/crown/discard-history flow as the UI package.

This UI revision removes the empty hand placeholder and improves the client-side deck settle geometry; no gameplay/scoring/timer rule was changed on the server.
