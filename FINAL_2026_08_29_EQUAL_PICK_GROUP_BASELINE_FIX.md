# Equal pick / grouped-hand baseline UI fix

Railway gameplay/server logic is unchanged from the matching reference draw-spacing build.
The UI now keeps non-dragged cards on one vertical baseline during group reflow and uses a non-spring equal tap-selection lift.
