# Matching Railway package

No server gameplay change was required for the random-hand / explicit SORT UI correction.
This Railway package preserves the matching authoritative server used by the final UI package, including timer sync, dealer/crown rotation, start animation and discard history.
