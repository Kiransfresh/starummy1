# Star Rummy production Socket.IO backend
VITE_BACKEND_URL=https://starummy1-production.up.railway.app

# Do not put private SMS/payment provider secrets in VITE_ variables.
# VITE_ values are bundled into the Android/web client and are public.
