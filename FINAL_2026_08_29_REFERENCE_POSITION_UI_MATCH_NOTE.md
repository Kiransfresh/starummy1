# Matching Railway Package

This backend is byte-identical to the server bundled with the final UI package.
The 2026-08-29 fixes are UI-only: Create Room safe-area/layout, single four-box room-code display, centered share hint, and permanent removal of the empty dotted `+` hand placeholder.
No server/gameplay change was required.
