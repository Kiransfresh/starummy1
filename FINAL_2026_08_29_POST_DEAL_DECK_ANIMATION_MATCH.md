# Post-deal deck animation matching note

The Railway timing remains authoritative and already waits for all 13-card flights before the deck setup window. This package keeps that timing synchronized with the UI build. The visual change is in the UI: closed/open/joker deck cards settle only after the final deal card lands, and the first turn starts after that setup window.
